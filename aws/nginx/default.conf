server {
    listen       80;
    server_name  names-app.click;

    root   /usr/share/nginx/html;
    index  index.html;

    location /nginx {
        rewrite ^/nginx(.*)$ /$1 break;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}